/** @type {import('next').NextConfig} */

 


const nextConfig = {
    i18n: {
        locales: ['fr', 'ar'],   // Liste des langues
        defaultLocale: 'ar',      // Langue par défaut
      },
};

export default nextConfig;
